# PathSense v2 — Vision dependencies
# Separate from main requirements.txt because ultralytics + torch
# are large and only needed by the vision service container.

ultralytics>=8.1
opencv-python-headless>=4.9
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.0
numpy>=1.24
Pillow>=10.0
python-multipart>=0.0.9
requests>=2.31
python-dotenv>=1.0
